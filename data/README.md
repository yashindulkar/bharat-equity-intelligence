# Data directory

No market, licensed, or personal data is committed. Future synthetic fixtures must use impossible identifiers and conspicuous `SYNTHETIC` labels. Provider contract fixtures require written licensing approval and redaction review.
